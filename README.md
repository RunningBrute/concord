# concord
A lightweight text analysis tool written in Rust, starting with word frequency analysis and evolving into a simple concordance.

# Commands
* run: `cargo run`
* profile: `cargo flamegraph`
* format: `rustfmt example/file.rs`
